# MySQL/MariaDB override — use with:
#   docker compose -f docker-compose.yml -f docker-compose.mysql.yml up -d

services:
  bookshelf:
    environment:
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: bookshelf
      DB_USERNAME: bookshelf
      DB_PASSWORD: "${DB_PASSWORD:-changeme}"
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mariadb:11
    container_name: bookshelf-db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: "${DB_ROOT_PASSWORD:-rootchangeme}"
      MARIADB_DATABASE: bookshelf
      MARIADB_USER: bookshelf
      MARIADB_PASSWORD: "${DB_PASSWORD:-changeme}"
    volumes:
      - bookshelf_db:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  bookshelf_db:
